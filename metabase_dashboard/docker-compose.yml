services:
  db:
    build: ./data/
    container_name: postgres_test
    ports:
      - 5432:5432
    volumes:
      - ./data/:/app

  metabase-app:
    image: metabase/metabase
    restart: always
    ports:
      - 3000:3000
    depends_on:
      - db

volumes:
  postgres-db:
    driver: local
